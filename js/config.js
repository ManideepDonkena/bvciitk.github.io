const CONFIG = {
    // Replace these URLs with your actual Google Sheet CSV published URLs
    // File -> Share -> Publish to web -> Select Sheet -> Comma-separated values (.csv)
    alumniUrl: 'mock_data/alumni.csv',
    teamUrl: 'mock_data/team.csv',
    eventsUrl: 'mock_data/events.csv'
};
